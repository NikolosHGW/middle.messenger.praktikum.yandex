{{#if withEditSpan}}
  <span class="edit-label__span">{{{spanText}}}</span>
{{/if}}
<input
  id="{{{inputId}}}"
  class="{{{inputClassName}}}"
  type="{{{inputType}}}"
  name="{{{inputName}}}"
  required
  minlength="2"
  maxlength="40"
  placeholder="{{{placeholder}}}"
>
<span class="{{{errorSpanClassName}}}"></span>
